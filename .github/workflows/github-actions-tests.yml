name: GitHub Actions Run Tests
run-name: ${{ github.actor }} is testing out Tests with GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "1\ Infos"
      - run: echo "Event name: ${{ github.event_name }}."
      - run: echo "OS: ${{ runner.os }}"
      - run: echo "Branch: ${{ github.ref }}  ${{ github.repository }}."
      - run: echo "Repository:  ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "2\ The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "3\ Tests:"
      - name: List files in the repository
        run: |
          sh run-test
      - run: echo "4\ Return value: ${{ job.status }}."
